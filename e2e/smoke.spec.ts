import {expect,test} from "@playwright/test";
test("관리자 로그인과 대시보드",async({page})=>{await page.goto("/login");await expect(page.getByRole("heading",{name:"연구소 로그인"})).toBeVisible();await page.getByRole("button",{name:"로그인"}).click();await expect(page).toHaveURL(/\/admin\/dashboard/);await expect(page.getByRole("heading",{name:/연구소장님/})).toBeVisible();await page.screenshot({path:"artifacts/screenshots/admin-dashboard-1440.png",fullPage:true})});
test("고객용 모바일 쿠지 화면",async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto("/store/hongdae");await expect(page.getByRole("heading",{name:/오늘은 어떤 행운/})).toBeVisible();await expect(page.getByText("지금 참여할 수 있는 쿠지")).toBeVisible();await page.screenshot({path:"artifacts/screenshots/store-mobile-390.png",fullPage:true})});
