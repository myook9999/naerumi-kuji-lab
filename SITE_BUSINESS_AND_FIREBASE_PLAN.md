# 사이트 판매 및 Firebase 연동 계획

작성일: 2026-07-22

## 1. 서비스 판매 방향

현재 사이트는 화면과 주요 흐름을 보여주는 시연판이다. 실제 판매는 Firebase 영구 저장, 인증, 지점 프로그램 연동과 운영 검증을 완료한 뒤 시작한다.

권장 판매 방식:

- 2주 무료 체험
- 정식 연동 설치비 50만원
- 지점 1곳 기준 월 이용료 7만 9천원

초기 지점 모집안:

- 선착순 테스트 지점은 설치비 30만원 또는 할인 제공
- 2주 동안 전체 기능 체험
- 계속 이용할 경우에만 설치비와 월 이용료 발생
- 체험 전에 유료 전환 가격을 명확하게 안내

설치비에 포함할 항목:

- 기존 쿠지 프로그램 연동
- Firebase 지점 등록
- 기존 데이터 초기 반영
- 관리자 계정과 지점 권한 설정
- 사용법 안내
- 초기 오류 대응

월 이용료에 포함할 항목:

- 서버 및 Firebase 운영
- 기본 데이터 백업
- 기능 업데이트
- 장애 확인
- 기본 기술 지원

권장 약관 문구:

> 월 79,000원에는 지점 1곳, 등록회원 2,000명 이하, 정상 범위의 데이터 사용량 및 기본 유지보수가 포함됩니다. 비정상적으로 많은 트래픽이나 대규모 데이터 사용 시 별도 협의합니다.

## 2. 사용자 1,000명 규모

Firebase 운영판으로 전환하고 조회 구조를 최적화하면 가입자 1,000명은 충분히 처리할 수 있다.

일반적인 판단:

- 가입자 1,000명, 일일 이용자 50~200명: 충분히 가능
- 동시 접속 50~100명: 일반적인 운영 범위
- 1,000명이 동시에 실시간 화면 사용: 별도 부하 테스트 필요
- 초당 수백 건 이상의 티켓 이벤트: 추가 설계 필요

사용자 수보다 다음 항목이 서버 비용과 성능에 더 큰 영향을 준다.

- 한 화면에서 읽는 문서 개수
- 실시간 구독 범위
- 티켓 판매와 당첨 이벤트 횟수
- 이미지와 파일 전송량
- 사용자의 재접속 빈도

월 7만 9천원은 일반적인 지점 1곳과 가입자 1,000명 수준에서 운영비를 감당할 가능성이 높다. 다만 Firebase 비용뿐 아니라 문의 대응, 백업, 오류 수정과 업데이트 시간도 월 이용료에 포함해야 한다.

필수 최적화:

- 지점별 데이터만 조회
- 목록 페이지네이션
- 판 전체가 아닌 변경 이벤트만 저장
- 대시보드 집계 문서 별도 운영
- 불필요한 실시간 구독 차단
- 이미지 압축과 캐시
- Firebase 예산 알림
- 출시 전 동시 접속 테스트

## 3. 기존 Firebase 연동 원칙

기존 Firebase 전체를 수정하지 않는다. 사이트를 신청한 지점 데이터만 연결한다.

가장 안전한 구조:

```text
기존 쿠지 프로그램
       ↓ 원본 데이터 저장
Firebase 기존 컬렉션
       ↓ 신청 지점의 필요한 변경분만 동기화
웹사이트 전용 컬렉션
       ↓ 읽기 전용
사장님·회원 웹사이트
```

예시:

```text
core/boards                   기존 프로그램 전용
core/tickets                  기존 프로그램 전용

webStores/store-1/dashboard   사이트 전용
webStores/store-1/boards      사이트 전용
webStores/store-1/winners     사이트 전용
```

홍대 본점만 사이트를 신청했다면 `store-1` 데이터만 동기화한다. 다른 지점은 조회하거나 수정하지 않는다.

## 4. 기존 프로그램과 다른 지점에 미치는 영향

사이트를 읽기 전용 및 웹 전용 컬렉션으로 분리하면:

- 다른 지점 데이터 변경 없음
- 기존 티켓 판매와 개봉 기능에 영향 없음
- 기존 프로그램 기능 변경 없음
- 기존 Owner API 부하 거의 없음
- 사이트에 장애가 발생해도 기존 프로그램은 계속 작동

같은 Firebase 프로젝트를 사용하는 경우 공통으로 증가할 수 있는 항목:

- Firestore 문서 읽기·쓰기 사용량
- 네트워크 사용량
- Firebase 청구 비용
- 프로젝트 공통 할당량

사이트가 기존 원본 문서를 직접 수정하면 발생할 수 있는 문제:

- 남은 티켓 수량 불일치
- 동일 티켓 중복 개봉
- 당첨 기록 중복 등록
- 정산 금액 오류
- 기존 프로그램 화면과 사이트 상태 충돌

따라서 기존 쿠지 프로그램을 원본 데이터의 유일한 작성자로 유지한다. 사이트에서 배송이나 설정을 수정해야 할 때는 기존 문서를 브라우저가 직접 수정하지 않고 보안 API를 거친다.

```text
사이트
→ 보안 API
→ 인증·지점 권한·중복 검증
→ 허용된 데이터만 수정
```

## 5. 전체 시스템에 영향을 줄 수 있는 위험 작업

다음 작업은 다른 지점에도 영향을 줄 수 있으므로 피한다.

- Firebase 보안 규칙 전체 허용
- 기존 컬렉션 삭제 또는 이름 변경
- 기존 문서 구조 일괄 변경
- 사이트에서 판 전체 데이터 덮어쓰기
- 모든 지점 데이터를 하나의 실시간 구독으로 조회
- 모든 컬렉션에 적용되는 광범위한 Cloud Function 배포

보안 규칙은 사이트용 경로와 신청 지점으로 제한한다.

```text
webStores/store-1/**
```

기존 핵심 데이터에 적용되는 규칙은 가능하면 수정하지 않는다.

## 6. 지점 1곳 테스트 순서

1. 기존 Firebase와 프로그램 데이터 백업
2. 테스트 환경 또는 웹 전용 컬렉션 생성
3. 신청 지점의 `storeId`만 허용
4. 사이트를 읽기 전용으로 먼저 연결
5. 쿠지판 재고와 당첨 기록 반영 확인
6. 기존 프로그램에서 판매·티켓 개봉 반복 테스트
7. Firebase 사용량과 오류 로그 확인
8. 배송 등 사이트 입력 기능을 보안 API로 연결
9. 일정 기간 안정화 후 다른 지점 추가

## 7. PC와 모바일 안내 기준

현재 사이트는 모바일 앱이 아니라 반응형 웹이다.

홍보 문구:

> PC와 모바일 브라우저에서 편하게 확인할 수 있도록 반응형 웹으로 제작하고 있어요!

실제 배포 전에는 아이폰 Safari와 안드로이드 Chrome 실기기 검수를 추가한다.

## 핵심 결론

- 기존 Firebase 전체를 수정할 필요 없음
- 사이트를 신청한 지점만 하나씩 연결
- 기존 원본 데이터는 직접 변경하지 않음
- 웹 전용 컬렉션과 보안 API 사용
- 가입자 1,000명은 최적화된 Firebase 운영판에서 충분히 대응 가능
- 설치비 50만원과 월 7만 9천원 정책은 실제 연동과 유지보수가 포함될 때 적절함

