"use client";
import {CircleDollarSign,Download,FileCheck,Receipt,Ticket,Truck,Undo2} from "lucide-react";import {useState} from "react";import {toast} from "sonner";import {Badge,Button,Card,Modal} from "@/components/ui";import {KpiCard,PageHeader,SectionTitle} from "./common";import {DonutChart,SalesChart} from "@/components/charts";import {hourlySales} from "@/lib/mock/data";import {useLab} from "@/components/providers";import {downloadCsv,won} from "@/lib/utils";
export function Settlements(){const lab=useLab(),settlement=lab.settlements.find(x=>x.storeId===lab.selectedStoreId)??lab.settlements[0],[memo,setMemo]=useState(settlement.memo),[tags,setTags]=useState<string[]>(settlement.tags),[open,setOpen]=useState(false);const net=settlement.totalSales-settlement.refund-settlement.fee+settlement.shipping;const exportCsv=()=>downloadCsv("settlement-2026-07-21.csv",settlement.items.map(x=>({"쿠지판":x.boardTitle,"판매 수량":x.sold,"매출":x.sales,"환불":x.refund,"정산 금액":x.amount,"상태":x.status})));return <><PageHeader title="오늘의 정산" description="지점별 매출과 수수료를 검토하고 일일 정산을 확정합니다." actions={<><input aria-label="정산 날짜" type="date" defaultValue="2026-07-21"/><Button variant="ghost" onClick={exportCsv}><Download size={15}/>엑셀 다운로드</Button><Button variant="ghost" onClick={()=>toast.success("리포트를 저장했습니다.")}><FileCheck size={15}/>리포트 저장</Button><Button disabled={settlement.confirmed} onClick={()=>setOpen(true)}>{settlement.confirmed?"정산 확정됨":"정산 확정"}</Button></>}/><div className="kpi-grid six"><KpiCard label="총 매출" value={won(settlement.totalSales)} note="+12.4%" icon={CircleDollarSign}/><KpiCard label="실 정산 금액" value={won(net)} note="예상 금액" icon={Receipt} tone="mint"/><KpiCard label="수수료" value={won(settlement.fee)} note="3.5%" icon={FileCheck} tone="orange"/><KpiCard label="환불·취소" value={won(settlement.refund)} note="4건" icon={Undo2} tone="pink"/><KpiCard label="배송비 정산" value={won(settlement.shipping)} note="14건" icon={Truck} tone="blue"/><KpiCard label="판매 티켓" value={settlement.ticketCount+"장"} note="+18장" icon={Ticket}/></div><Card><SectionTitle title="쿠지판별 정산" sub="쿠지판 단위의 판매 및 지급 내역"/><div className="table-wrap"><table><thead><tr><th>쿠지판명</th><th>판매 수량</th><th>매출</th><th>상위상 지급</th><th>환불</th><th>정산 금액</th><th>상태</th></tr></thead><tbody>{settlement.items.map(x=><tr key={x.id}><td><b>{x.boardTitle}</b></td><td>{x.sold}장</td><td>{won(x.sales)}</td><td>{x.topPrizes}건</td><td className="danger">{won(x.refund)}</td><td><b>{won(x.amount)}</b></td><td><Badge tone={settlement.confirmed?"green":"orange"}>{settlement.confirmed?"확정":"검토 중"}</Badge></td></tr>)}</tbody></table></div></Card><div className="two-col"><Card><SectionTitle title="시간대별 매출 추이" action={<div className="segmented"><button className="active">매출</button><button>정산 금액</button></div>}/><SalesChart data={hourlySales}/></Card><Card><SectionTitle title="결제 수단별 내역"/><div className="donut-layout"><DonutChart data={[{name:"카드 결제",value:68},{name:"현금 결제",value:18},{name:"포인트",value:10},{name:"쿠폰",value:4}]}/><div className="legend-list"><div><i className="purple"/>카드 결제 <b>68%</b></div><div><i className="mint"/>현금 결제 <b>18%</b></div><div><i className="blue"/>포인트 <b>10%</b></div><div><i className="orange"/>쿠폰 <b>4%</b></div></div></div></Card></div><div className="two-col"><Card><SectionTitle title="정산 메모" sub="특이사항을 감사 기록과 함께 보관합니다."/><textarea value={memo} onChange={e=>setMemo(e.target.value)} placeholder="오늘의 이벤트, 재고 및 운영 특이사항을 기록하세요."/><div className="tag-row">{["이벤트 진행","재고 부족","일반 운영","기타"].map(x=><button onClick={()=>setTags(v=>v.includes(x)?v.filter(t=>t!==x):[...v,x])} className={tags.includes(x)?"active":""} key={x}>{x}</button>)}</div><Button variant="ghost" onClick={()=>toast.success("정산 메모를 저장했습니다.")}>메모 저장</Button></Card><Card className="final-summary"><SectionTitle title="정산 요약"/>{[["총 매출",settlement.totalSales],["환불·취소",-settlement.refund],["수수료",-settlement.fee],["배송비",settlement.shipping]].map(x=><div key={String(x[0])}><span>{x[0]}</span><b>{won(Number(x[1]))}</b></div>)}<footer><span>실 정산 금액</span><strong>{won(net)}</strong></footer></Card></div><Modal open={open} onClose={()=>setOpen(false)} title="오늘 정산을 확정할까요?"><p>확정 후에는 담당 권한자가 아니면 수정할 수 없습니다. 감사 로그가 자동으로 기록됩니다.</p><div className="modal-actions"><Button variant="ghost" onClick={()=>setOpen(false)}>취소</Button><Button onClick={()=>{lab.confirmSettlement(settlement.id,memo,tags);setOpen(false)}}>정산 확정</Button></div></Modal></>}
