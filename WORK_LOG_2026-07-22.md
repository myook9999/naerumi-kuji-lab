# 내루미의 쿠지연구소 작업일지 — 2026-07-22

## 작업 위치

`D:\codexstudy\naerumi-kuji-lab`

## 오늘 완료한 작업

### 공통 안정화

- 기존 TypeScript 오류 전부 수정
- CSV 다운로드가 표 배열과 객체 배열을 모두 처리하도록 개선
- 깨진 원화 표기와 접근성 한글 문자열 수정
- 회원의 지점 필드를 `storeId` 기준으로 통일
- Firebase 저장소 인터페이스 반환 타입 정리
- 작업 전 소스 백업 생성
  - `C:\Users\myook\codex-work\naerumi-kuji-lab-backup-20260722-225236`

### 관리자 사이트

- 관리자 공통 레이아웃과 전체 메뉴 연결
- 연구소 대시보드
- 오늘의 정산
- 쿠지판 관찰실
- 상위상 기록실
- 회원·배송 관리
- 포인트 강화실
- 지점 연구소 관리
- 상품·쿠지 관리
- 정산 리포트
- 알림·메시지
- 연구소 설정

대시보드 인사말은 선택된 지점에 따라 아래처럼 표시되도록 수정했다.

- `홍대 본점 사장님!`
- `건대점 사장님!`

### 인증 및 고객 화면

- 로그인
- 회원가입
- 비밀번호 재설정
- 고객용 지점 페이지 `/store/[storeSlug]`
- PC·태블릿·390px 모바일 반응형 레이아웃
- 자체 브랜드 로고와 심볼 SVG 추가
- 기본 Next.js 템플릿 자산 제거

### 데모 동작

- 브라우저 `localStorage`를 이용한 상태 저장
- 지점 전환
- 정산 확정 및 중복 확정 방지
- 배송 상태와 송장 번호 수정
- 회원 정보 수정
- 쿠지판 등록
- 포인트 강화와 보상 수령
- 알림 읽음 처리
- 데모 데이터 초기화

### 서버 API 예시

- `POST /api/enhancement/attempt`
  - 서버 확률 계산
  - 포인트 차감
  - 일일 횟수 제한
  - 요청 중복 방지
- `GET /api/integrations/health`
- `POST /api/integrations/sync`
- `POST /api/integrations/events`
  - HMAC-SHA256 서명
  - 요청 시간 검증
  - Zod 입력 검증
  - 이벤트 중복 방지

## 검증 결과

- ESLint: 통과
- TypeScript strict 검사: 통과
- Vitest: 2개 파일, 6개 테스트 통과
- Next.js 프로덕션 빌드: 22개 경로 모두 성공
- Playwright Chromium: 2개 E2E 테스트 통과
  - 관리자 로그인 및 대시보드
  - 390px 고객용 모바일 화면

스크린샷:

- `artifacts/screenshots/admin-dashboard-1440.png`
- `artifacts/screenshots/store-mobile-390.png`

## 실행 방법

```powershell
cd D:\codexstudy\naerumi-kuji-lab
corepack pnpm dev
```

- 로그인: http://localhost:3000/login
- 고객 화면: http://localhost:3000/store/hongdae
- 관리자: `admin@naerumi.test / demo1234`
- 지점 관리자: `manager@naerumi.test / demo1234`
- 회원: `member@naerumi.test / demo1234`

## Firebase 연동 방향

지점 한 곳부터 Firebase에 연결할 수 있다. 권장 흐름은 다음과 같다.

```text
지점 쿠지 프로그램
→ 변경 이벤트만 사이트 연동 API로 전송
→ Firestore에 지점별 저장
→ 웹사이트가 실시간 구독으로 반영
```

판 전체를 매번 저장하지 않고 티켓 개봉, 당첨 발생, 재고 변경, 정산 완료와 같은 변경분만 전송한다. 데이터는 `storeId` 기준으로 나누며 테스트 지점이 안정화된 뒤 같은 구조로 다른 지점을 추가한다.

## 시연판과 실제 납품판

현재 버전은 홍보와 화면 시연을 위한 데모다. 화면 흐름과 주요 버튼은 작동하지만 데이터는 목업과 브라우저 저장소를 사용한다.

실제 납품 전 필요한 작업:

1. Firebase 운영 프로젝트와 영구 데이터베이스 연결
2. Firebase Auth 및 서버 권한 검증
3. 지점 프로그램의 실제 데이터 형식 확인
4. 결제·택배사 API 연결
5. 정산·포인트·배송 감사 로그 영구 저장
6. Firestore Security Rules와 인덱스 배포
7. 아이폰 Safari와 안드로이드 Chrome 실기기 검수

자세한 사항은 `docs/PRODUCTION_CHECKLIST.md`를 참고한다.

판매 가격, 사용자 1,000명 규모, Firebase 지점별 연동과 기존 프로그램 영향에 대한 상세 계획은 `SITE_BUSINESS_AND_FIREBASE_PLAN.md`에 정리되어 있다.

## 홍보 문구 기준

PC와 모바일을 모두 지원한다는 표현은 모바일 앱이 아니라 반응형 웹을 뜻한다. 홍보할 때는 아래 문구를 사용한다.

> PC와 모바일 브라우저에서 편하게 확인할 수 있도록 반응형 웹으로 제작하고 있어요!
